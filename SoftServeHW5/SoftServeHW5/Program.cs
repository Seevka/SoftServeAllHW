using System;
using System.Collections.Generic;
using System.Linq;

namespace SoftServeHW5
{
    class Program
    {
        static void Main(string[] args)
        {
            Console.WriteLine(int.Parse("kads"));
            //List<string> myarrayForProg = new List<string>();
            //List<string> myarrayForBuild = new List<string>();
            //IDeveloper developer = new Builder();
            //IDeveloper developer_ = new Programmer();
            //Console.WriteLine("Work with programmers");
            //for (int i = 0; i < 2; i++)
            //{
            //    developer_.Create(myarrayForProg);
            //}
            //developer_.OutPut(myarrayForProg);
            //developer_.Destroy(myarrayForProg);
            //developer_.OutPut(myarrayForProg);
            //Console.WriteLine("Work with builders");
            //for (int i = 0; i < 2; i++)
            //{
            //    developer.Create(myarrayForBuild);
            //}
            //developer.OutPut(myarrayForBuild);
            //developer.Destroy(myarrayForBuild);
            //developer.OutPut(myarrayForBuild);

            //Dictionary<uint, string> myDict = new Dictionary<uint, string>();
            //for(int i=0;i<7;i++)
            //{
            //    Console.WriteLine($"Enter id of {i} user");
            //    var userId = uint.Parse(Console.ReadLine());
            //    while(myDict.ContainsKey(userId))
            //    {
            //        Console.WriteLine("This id exists,try another one!");
            //        userId = uint.Parse(Console.ReadLine());
            //    }
            //    Console.WriteLine($"Enter name of {i} user");
            //    var userName = Console.ReadLine();
            //    myDict.Add(userId, userName);
            //}
            //Console.WriteLine("Enter Id of the user u wanna to find: ");
            //var userSearch = uint.Parse(Console.ReadLine());
            //var foundUserName= myDict.FirstOrDefault(x => x.Key ==userSearch ).Value;
            //if(foundUserName==null)
            //{
            //    Console.WriteLine("No such user!");
            //}
            //else
            //{
            //    Console.WriteLine($"Your user with {userSearch} Id is: {foundUserName}");
            //}
        }
    }
}
